export interface People {
    firstName: string,
    secondName: string,
    age: string,
    img?: string,
    adress?: string,
    id?: string | number,
    ownerId?: string | number,
    creationDate?: Date
    ownerEmail?: string
}